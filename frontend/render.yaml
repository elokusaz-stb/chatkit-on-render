services:
  - type: web
    name: chatkit-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run serve
    envVars:
      - key: VITE_API_BASE_URL
        value: https://chatkit-backend.onrender.com
